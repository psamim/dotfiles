#!/usr/bin/env python

while(1):
    s = str(input())
    if (len(s) > 40):
       s = s[:30] + '..' + s[-10:] #cut right from center
       # s = s[:30] + '..' + s[-1:] #cut normal      
    print(s)

